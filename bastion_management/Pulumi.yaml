name: bastion-auto-shutdown-app
runtime:
  name: python
  options:
    toolchain: poetry
description: Bastion automated shutdown and invoke by PaaS
config:
  pulumi:tags:
    value:
      pulumi:template: azure-python
